{{ define "index.tmpl" }}
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ .title }}</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="{{ .css_resets }}" rel="stylesheet" />
  </head>
<body>
</body>
</html>
{{ end }}
